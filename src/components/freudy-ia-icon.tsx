
import { cn } from "@/lib/utils";
import { BrainCircuit } from "lucide-react";

export function FreudyIAIcon({ className }: { className?: string }) {
  return (
    <BrainCircuit className={cn(className)} />
  );
}
